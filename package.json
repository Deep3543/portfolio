{
    "name": "portfolio",
    "version": "1.0.0",
    "homepage": "./",
    "private": true,
    "dependencies": {
        "@testing-library/react": "^11.1.0",
        "animate.css": "^4.1.1",
        "react": "^17.0.2",
        "react-dom": "^17.0.2",
        "react-scripts": "^5.0.1",
        "react-scroll": "^1.8.4",
        "web-vitals": "^1.0.1"
    },
    "scripts": {
        "start": "react-scripts start",
        "build": "react-scripts build",
        "predeploy": "npm run build",
        "deploy": "npm run build && git add . && git commit -m 'Deploying to master branch' && git push origin master",
        "test": "react-scripts test",
        "eject": "react-scripts eject"
    },
    "eslintConfig": {
        "extends": [
            "react-app",
            "react-app/jest"
        ]
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
